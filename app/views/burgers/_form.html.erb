<%= semantic_form_for @burger, :html => { :multipart => true } do |f| %>
  <% if @burger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@burger.errors.count, "error") %> prohibited this burger from being saved:</h2>

      <ul>
      <% @burger.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= f.inputs do %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :brand, :collection => Brand.all %>
    <%= f.input :photo, :as => :file %>
    <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
